name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and start services
        run: |
          docker compose build
          docker compose up -d gitea
          echo "[CI] Waiting for Gitea to become ready..."
          docker exec tester bash /app/scripts/wait_for_gitea.sh
          docker compose up -d tester

      - name: Run tests inside tester
        run: |
          docker exec tester pytest -v -n auto --alluredir=/app/allure-results

      - name: Copy allure results
        run: docker cp tester:/app/allure-results ./allure-results

      - name: Upload allure results
        uses: actions/upload-artifact@v4
        with:
          name: allure-results
          path: ./allure-results

  deploy-report:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download allure results
        uses: actions/download-artifact@v4
        with:
          name: allure-results
          path: ./allure-results

      - name: Generate Allure Report
        run: |
          pip install allure-pytest
          allure generate ./allure-results -o ./allure-report --clean

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./allure-report
